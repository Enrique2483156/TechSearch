 
To do:
search by zipcode, email, name (contacts) and the nearest techs (mesurment change km to miles count in minute and check by route)
store into the db zoho crm contacts, synch prefered techs to contact into zoho crm contacts
create cron task for fetching zoho fsm tech and zoho crm contacts (if new add or update if exist)
schedule synch for zoho fsm tech

To do in frontend:
search by email, name (contacts)
/*
To do:
store into the db zoho crm contacts, (on-off process)
store into the db zoho fsm contacts, ()
store service addresses in the db fsm_contacts from zoho fsm

synch prefered techs to contact into zoho crm contacts
create cron task for fetching zoho fsm tech and zoho crm contacts (if new add or update if exist)
schedule synch for zoho fsm tech
*/
1. Search by ZIP code in frontend
2. LOGGING:
        -error
        -success
        -info

3. Dev branch
4. Check if there is a contact with the same email address when creating contact or lead